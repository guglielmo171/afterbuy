// ponytail: placeholder — real tests will live in entities/ and shared/.
import { describe, it, expect } from "vitest";

describe("scaffold", () => {
  it("project is set up", () => {
    expect(true).toBe(true);
  });
});